{
  "name": "gass-git",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "./src/index.html",
  "scripts": {
    "build": "rimraf dist && vite build && blazed build",
    "dev": "nodemon --watch src --ext ts,json,css,html --exec \"npm run build && vite preview\""
  },
  "devDependencies": {
    "blazed-past-us": "^0.2.9",
    "nodemon": "^3.1.11",
    "rimraf": "^6.1.2",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "three": "^0.182.0"
  }
}
